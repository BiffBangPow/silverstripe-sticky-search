# Silverstripe Stick Search

A simple module to make the search bar in Silverstripe CMS sticky

## Installation
Just run: 
`composer require biffbangpow/silverstripe-sticky-search`
Followed by:
`composer expose-vendor`
